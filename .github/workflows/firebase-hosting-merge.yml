# .github/workflows/firebase-hosting-merge.yml
# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge

on:
  push:
    branches:
      - main # This workflow runs when code is pushed to the 'main' branch

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest # GitHub Actions runner environment
    steps:
      - uses: actions/checkout@v4 # Checks out your repository code
      - run: npm ci && npm run build # This is the command that failed previously
        # IMPORTANT FIX: Added 'working-directory' to run npm commands from the client app's directory
        working-directory: ./client # <--- This line has been added/corrected
                                    #      Assumes your React app's package.json is in a 'client' folder
      - uses: FirebaseExtended/action-hosting-deploy@v0 # Firebase Hosting deployment action
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_BOOKENDS_E027A }}'
          projectId: bookends-e027a
          channelId: live
